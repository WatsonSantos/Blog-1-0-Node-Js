<div class="card mb-5">
    <div class="card-body mb-5">
        <h1>{{titulo}}</h1>
        <hr>
        <small>Datade publicação: {{data}}</small>
        <hr>
        {{conteudo}}
    </div>
</div>
